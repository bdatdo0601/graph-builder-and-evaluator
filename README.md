Based on [VAX](https://github.com/evazamtz/vax)

To run you need Ruby & Jekyll

```
brew install ruby
sudo gem install jekyll bundler
```

To Serve

```
bundle exec jekyll serve
```

it should be available at `localhost:4000`
